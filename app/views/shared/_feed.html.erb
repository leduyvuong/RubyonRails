<% if @feed_items.any? %>
  <ol class = "microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: {controller: :static_pages, action: :home}, :previous_label => t("labelw.prev_label"), :next_label => t("labelw.next_label") %>
<% end %>